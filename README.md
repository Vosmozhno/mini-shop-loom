# 🛍️ Mini Shop (Loom)

Минималистичный интернет-магазин, построенный на архитектуре Headless Commerce.
Проект реализует полный цикл покупки: от просмотра каталога и поиска до оформления заказа.

🚀 **[Открыть магазин](https://mini-shop-loom.vercel.app)**

## 🛠 Технический стек

### Frontend
- **Framework:** Next.js 15 (App Router)
- **Styling:** Tailwind CSS
- **State Management:** React Context API + LocalStorage
- **Deployment:** Railway

### Backend & CMS
- **E-commerce Engine:** MedusaJS (v2) — управление товарами, корзиной и заказами.
- **Headless CMS:** Strapi (v4) — управление контентом (Главная страница, "О нас").
- **Database:** PostgreSQL (используется и для Medusa, и для Strapi в продакшене).
- **Hosting:** Railway

---

## 📂 Архитектура и Деплой

Проект развернут на платформе **Railway** и состоит из трех связанных сервисов:

1.  **Frontend (Next.js):** Получает данные от обоих бэкендов и рендерит страницы.
2.  **Strapi CMS:** Хранит текстовый и медиа-контент. Использует PostgreSQL.
3.  **Medusa Backend:** Обрабатывает бизнес-логику магазина. Использует PostgreSQL.

---

## 💻 Локальный запуск

Для запуска проекта локально потребуется **Node.js (v18+)** и запущенный **PostgreSQL**.

### Шаг 1: Настройка CMS (Strapi)

1.  Перейдите в папку: `cd cms`
2.  Установите зависимости: `npm install`
3.  Запустите: `npm run develop`
    *   *Админ-панель:* `http://localhost:1337/admin`

### Шаг 2: Настройка Backend (Medusa)

1.  Перейдите в папку: `cd backend-medusa`
2.  Установите зависимости: `npm install`
3.  Настройте `.env` (укажите локальную БД Postgres).
4.  Запустите миграции: `npx medusa migrations run`
5.  Запустите сервер: `npm start`
    *   *API:* `http://localhost:9000`

### Шаг 3: Запуск Frontend (Next.js)

1.  Перейдите в папку: `cd frontend`
2.  Установите зависимости: `npm install`
3.  Создайте `.env.local`:
    ```env
    # Для локальной разработки:
    NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
    NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
    ```
4.  Запустите: `npm run dev`
    *   *Сайт:* `http://localhost:3000`

---

## ✅ Функционал

1.  **Каталог:** Интеграция с Medusa Store API, фильтрация, поиск.
2.  **Контент:** Динамические данные с Strapi (заголовки, баннеры).
3.  **Корзина:** Глобальный стейт, сохранение при перезагрузке.
4.  **Чекаут:** Валидация форм, выбор доставки, создание заказа.
5.  **SEO:** Server Side Metadata.

---

## 📂 Структура репозитория

```text
├── backend-medusa/    # Medusa v2 (Логика магазина)
├── cms/               # Strapi v4 (Контент + конфиг для PostgreSQL)
└── frontend/          # Next.js (Клиентское приложение)