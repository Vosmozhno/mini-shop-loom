# 🛍️ Mini Shop (Loom)

Минималистичный интернет-магазин, построенный на архитектуре Headless Commerce.
Проект реализует полный цикл покупки: от просмотра каталога и поиска до оформления заказа без онлайн-оплаты.

## 🛠 Технический стек

### Frontend
- **Framework:** Next.js 15 (App Router)
- **Styling:** Tailwind CSS
- **State Management:** React Context API + LocalStorage
- **Icons:** Lucide React
- **Features:** Client-side fetching, Server Side SEO metadata, Responsive Design, Form Validation.

### Backend & CMS
- **E-commerce Engine:** MedusaJS (v2) — модульная архитектура для управления товарами, корзиной, заказами и фулфилментом.
- **Headless CMS:** Strapi (v4) — управление контентом (Главная страница, блок "О нас").
- **Database:** PostgreSQL (для Medusa), SQLite (для Strapi по умолчанию).

---

## 🚀 Инструкция по запуску

Для запуска проекта потребуется **Node.js (v18+)** и запущенный локально **PostgreSQL**.
Проект состоит из трех частей, которые нужно запустить в отдельных терминалах.

### Шаг 1: Настройка и запуск Backend (Medusa)

Этот сервис отвечает за логику магазина и API.

1. Перейдите в папку бэкенда:
   ```bash
   cd backend-medusa
   ```
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Создайте файл `.env` (скопируйте из `.env.template`) и убедитесь, что `DATABASE_URL` указывает на вашу базу PostgreSQL.
4. Выполните миграции и наполните базу тестовыми данными:
   ```bash
   npx medusa migrations run
   npx medusa seed --seed-file=data/seed.json
   ```
   *Это создаст дефолтного пользователя (admin@medusa-test.com / supersecret), регионы и товары.*

5. Запустите сервер:
   ```bash
   npm start
   ```
   *Сервер запустится на: `http://localhost:9000`*

### Шаг 2: Настройка и запуск CMS (Strapi)

Этот сервис отдает контент для главной страницы.

1. Перейдите в папку CMS:
   ```bash
   cd cms
   ```
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Запустите Strapi в режиме разработки:
   ```bash
   npm run develop
   ```
   *Админ-панель: `http://localhost:1337/admin`*

   > **Настройка контента:** Если база Strapi чистая, создайте `Single Type` с названием `Home`. Добавьте поля: `title` (Text), `content` (Rich Text / Blocks), `hero_image` (Media). Заполните данными и нажмите **Publish**.
   > В настройках **Settings -> Roles -> Public** разрешите доступ к методу `find` для `Home`.

### Шаг 3: Запуск Frontend (Next.js)

Клиентская часть магазина.

1. Перейдите в папку фронтенда:
   ```bash
   cd frontend
   ```
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Создайте файл `.env.local` в папке `frontend` и добавьте настройки:
   ```env
   # URL бэкенда Medusa
   NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000

   # URL CMS Strapi
   NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

   # (Опционально) ID канала продаж. Если не указан, Medusa использует дефолтный.
   # NEXT_PUBLIC_SALES_CHANNEL_ID=...
   ```
4. Запустите проект:
   ```bash
   npm run dev
   ```
   *Сайт доступен по адресу: `http://localhost:3000`*

---

## ✅ Реализованный функционал

1.  **Каталог товаров:**
    *   Интеграция с Medusa Store API.
    *   Фильтрация по категориям.
    *   Живой поиск.

2.  **Карточка товара:**
    *   Динамические страницы (`/product/[handle]`).
    *   Выбор количества.
    *   SEO-оптимизация (метатеги генерируются на сервере через `generateMetadata`).

3.  **Корзина и Чекаут:**
    *   Глобальный стейт корзины (Context API).
    *   Валидация формы оформления заказа (кастомная логика + маска телефона).
    *   Выбор методов доставки (интеграция с Fulfillment Options).
    *   Создание заказа и переход на страницу "Спасибо".

4.  **UI/UX:**
    *   Адаптивный дизайн.
    *   Стеклянный хедер.

---

## 📂 Структура проекта

```text
├── backend-medusa/    # E-commerce Engine (Medusa v2)
├── cms/               # Headless CMS (Strapi)
└── frontend/          # Клиентское приложение (Next.js)
    ├── src/app        # App Router, Страницы, SEO
    ├── src/components # Переиспользуемые UI компоненты
    ├── src/context    # Логика корзины (CartContext)
    └── src/lib        # Конфигурация клиентов API
```